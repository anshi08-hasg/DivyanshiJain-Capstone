flask>=3.0
flask-cors>=4.0
python-dotenv>=1.0
waitress>=3.0

# Only needed for the provider you actually set LLM_PROVIDER to.
# Safe to leave both installed; neither is imported unless selected.
# openai also serves Groq (OpenAI-compatible API, see llm_providers.py).
anthropic>=0.40
openai>=1.50

# Needed only for the "Push to FigJam" feature (figjam/figma_mcp_client.py).
# Local mode also requires Node.js/npx installed separately (spawns
# figma-console-mcp). Cloud mode (FIGJAM_MCP_MODE=cloud) needs no Node.js,
# just httpx for the HTTPS relay connection - used explicitly here even
# though mcp already depends on it, so the dependency isn't implicit.
mcp>=1.0
httpx>=0.27
